{
  "configurations": [
    {
      "name": "Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/paint-game",
      "cwd": "${workspaceRoot}",
      "env": {
        "RUST_LOG": "debug"
      }
    }
  ]
}